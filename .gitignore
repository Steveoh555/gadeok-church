# ─────────────────────────────────────────────
# 이 저장소에는 "웹사이트에 필요한 파일"만 올립니다.
# 아래 항목은 교회 내부 작업용이라 공개 저장소에 올리지 않습니다.
# (원본은 PC와 구글 드라이브에 그대로 있습니다)
# ─────────────────────────────────────────────

# 주보 원본 보관 폴더 (약 49MB, 구글 드라이브가 원본)
주보/

# 사진첩 원본 폴더 (홈페이지에는 줄인 사진 assets/photos/ 만 올라갑니다)
사진첩/

# 내부 작업 도구와 문서
#  - tools/config.json 에 교회 공유 드라이브 폴더 주소가 들어 있어 공개하지 않습니다
tools/
CLAUDE.md
사용법.md
작업정리.md
주보업데이트.bat

# 사이트는 js/data.js 만 읽으므로 원본 데이터는 올리지 않습니다
data/

# 잡파일
desktop.ini
.DS_Store
Thumbs.db
__pycache__/
*.pyc

# 배포 스크립트(로컬 전용)
홈페이지올리기.bat

# Claude 작업 설정(로컬 전용)
.claude/
